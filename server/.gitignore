# Ignorar archivos generados por el sistema operativo
.DS_Store
Thumbs.db

# Ignorar archivos de respaldo generados por editores de texto
*~

# Ignorar carpetas específicas
/node_modules
/build

/uploads/*
!/uploads/.gitignore
# Ignorar archivos de entorno (.env)
.env

# --- Seguridad / Credenciales ---
# Bloquear cualquier archivo JSON de Firebase Admin SDK en cualquier carpeta
**/*firebase-adminsdk-*.json

# Bloquear archivos JSON específicos en la carpeta server (por si acaso cambia el nombre)
server/*.json

# Excepciones: Permitir package.json y package-lock.json en server (¡IMPORTANTE!)
!server/package.json
!server/package-lock.json